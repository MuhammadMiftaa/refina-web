/**
 * Runtime Environment Configuration Template
 *
 * Placeholder $API_URL will be replaced at container startup.
 * During development, the fallback in src/lib/const.ts is used.
 */
window.__ENV__ = {
  API_URL: "$API_URL",
};
